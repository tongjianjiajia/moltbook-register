name: M
on: [push]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "=== Moltbook Status ==="
          curl -s "https://www.moltbook.com/api/v1/agents/status" \
            -H "Authorization: Bearer ${{ secrets.MOLTBOOK_API_KEY }}"
          echo ""
          echo "=== Search ==="
          curl -s "https://www.moltbook.com/api/v1/search?q=AI&limit=3" \
            -H "Authorization: Bearer ${{ secrets.MOLTBOOK_API_KEY }}"
