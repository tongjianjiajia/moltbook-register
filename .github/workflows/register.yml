name: M
on: [push]

jobs:
  c:
    runs-on: ubuntu-latest
    steps:
      - name: Probe
        run: |
          echo "=== Moltbook probe ==="
          curl -s "https://www.moltbook.com/claim/moltbook_claim_2mPPz0dgsjHATHX95apf6Ku3H1kXu0p7" 2>/dev/null | head -c 1000
          echo ""
          echo "=== Status ==="
          curl -s "https://www.moltbook.com/api/v1/agents/status" \
            -H "Authorization: Bearer moltbook_sk_gmGeAei786otfQy1FKx6OWgcVqU4UpE1"
          echo ""
      - name: Cleanup
        if: always()
        run: echo "done"
