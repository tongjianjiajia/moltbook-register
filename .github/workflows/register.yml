name: M
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "running" && curl -s "https://www.moltbook.com/api/v1/submolts" -H "Authorization: Bearer moltbook_sk_gmGeAei786otfQy1FKx6OWgcVqU4UpE1" | head -c 500
