name: MC
on: [push]
jobs:
  c:
    runs-on: ubuntu-latest
    steps:
      - run: |
          KEY="${{ secrets.MOLTBOOK_API_KEY }}"
          EMAIL="2864663178@qq.com"
          T="moltbook_claim_2mPPz0dgsjHATHX95apf6Ku3H1kXu0p7"
          
          echo "=== Send verification email ==="
          curl -s -X POST "https://www.moltbook.com/api/v1/agents/verify-email" \
            -H "Content-Type: application/json" \
            -d "{\"email\":\"${EMAIL}\",\"username\":\"DaHu_Jerry\",\"claim_token\":\"${T}\"}"
          echo ""
          
          echo "=== Status ==="
          curl -s "https://www.moltbook.com/api/v1/agents/status" -H "Authorization: Bearer $KEY"
