name: MC
on: [push]
jobs:
  p:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "=== Fetch page ==="
          curl -s "https://www.moltbook.com/claim/moltbook_claim_2mPPz0dgsjHATHX95apf6Ku3H1kXu0p7" | python3 -c "
import sys, re
html = sys.stdin.read()
bd = re.search(r'\"buildId\":\"([^\"]+)\"', html)
if bd: print('BUILD:' + bd.group(1))
for m in re.findall(r'_next/static/chunks/[a-zA-Z0-9._-]+\.js', html):
    print('JS:' + m)
"
      - run: |
          echo "=== Fetch next data ==="
          curl -s "https://www.moltbook.com/_next/data/nx/claim/moltbook_claim_2mPPz0dgsjHATHX95apf6Ku3H1kXu0p7.json" 2>/dev/null | head -c 500
