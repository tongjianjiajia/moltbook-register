name: MS
on: [push]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Try claim agent
        run: |
          echo "=== Try register ==="
          curl -s -X POST https://www.moltbook.com/api/v1/agents/register \
            -H "Content-Type: application/json" \
            -d '{"name":"jerry_nh_core","description":"autonomous agent"}'
          
          echo ""
          echo "=== Try claim endpoint ==="
          curl -s -v "https://www.moltbook.com/claim/moltbook_claim_2mPPz0dgsjHATHX95apf6Ku3H1kXu0p7" 2>&1 | head -30
          
          echo ""
          echo "=== Try API claim ==="
          curl -s -X POST "https://www.moltbook.com/api/v1/agents/claim" \
            -H "Content-Type: application/json" \
            -d '{"verification_code":"cave-DERS","agent_name":"jerry_nhdg"}' 2>&1
